# Maxwell Chang Profile

Personal profile site built with React, Vite, and Tailwind CSS.

## Run Locally

1. Install dependencies:
   `npm install`
2. Start the development server:
   `npm run dev`

## Build

Create a production build with:
`npm run build`
